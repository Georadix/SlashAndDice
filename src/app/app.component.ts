import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: '<slash-map></slash-map>',
  styles: [`
    :host {
      height: 100%;
    }
    
  `]
})
export class AppComponent {
}
